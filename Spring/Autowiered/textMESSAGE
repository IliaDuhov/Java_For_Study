Любая поле, метод или конструктор проаннотированные в классе бина (неважно будет ли в итоге бин получен через @Bean или @Component) будут объектом внедрения зависимости, 
при этом правила внедрения будут такими же как описано ранее (отбор по типу, имени, тегам, списки бинов).

Если requiered = false, то это даёт возможность перегрузки конструкторов. Если ни одна зависимость не будет найдена, то будет использован
конструктор по умолчанию(пустой), который надо прописать. 
